[defaults]
inventory           = inventory.yml
host_key_checking   = False
retry_files_enabled = False
stdout_callback     = default
result_format       = yaml
# La connexion utilise community.docker.docker (docker exec), pas SSH
# become=False car les conteneurs tournent en root

[privilege_escalation]
become              = False
