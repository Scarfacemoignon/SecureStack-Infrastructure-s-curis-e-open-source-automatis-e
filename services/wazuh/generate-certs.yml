version: "3.9"

services:
  generator:
    image: wazuh/wazuh-certs-generator:0.0.2
    hostname: wazuh-certs-generator
    container_name: wazuh-certs-generator
    volumes:
      - ./config.yml:/config/certs.yml
      - wazuh_certs:/certificates

volumes:
  wazuh_certs:
    name: tp_wazuh_certs
